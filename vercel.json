{
    "version": 2,
    "builds": [
        {
            "src": ".app.js",
            "use": "@vercel/node"
        }
    ],
    "rewrites": [
        { "rewrites": [{ "source": "/(.*)", "destination": "/" }] }
    ]
}